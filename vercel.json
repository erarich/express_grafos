{
  "version": 2,
  "builds": [
      {
          "src": "./Server.js",
          "use": "@vercel/node"
      }
  ],
  "routes": [
      {
          "src": "/(.*)",
          "dest": "/"
      }
  ],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}